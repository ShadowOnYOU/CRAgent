# Architecture

This sample project is intentionally small.

- `samplelib.text.normalize_whitespace` provides a tiny, deterministic utility.
- `samplelib.lru.LRUCache` is a thread-safe cache with basic stats.
