关键问题（高优先级）

lru.py:61 的 get_or_set() 现在是“先 get()（加锁）→ 释放锁 → factory() → 再 set()（加锁）”。这不是一个原子操作。
在并发场景下会出现：
factory() 被执行多次（重复计算/重复 IO/重复写入等副作用）。
“后写覆盖先写”：线程 A、B 同时 miss，各自算出不同 value，后 set() 的会覆盖前者；如果 value 不是幂等/确定性的，会造成数据不一致。
stats() 的 miss/hit 统计在并发下会被放大（不一定致命，但会误导监控/调参）。
